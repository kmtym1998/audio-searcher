# xena-web

## Requirements

- Node.js
  - `v20.3.1`
- pnpm
  - v8 系
  - npm 経由でのインストールを推奨します。 `npm install -g pnpm@latest`

## 環境構築

```sh
# 依存関係のインストール
$ pnpm install

# Next.js のサーバ立ち上げ
$ pnpm dev
```
